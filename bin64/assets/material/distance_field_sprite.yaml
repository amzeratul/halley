---
name: Distance field sprite
uniforms:
  - u_mvp: mat4
  - tex0: sampler2D
  - u_smoothness: float
  - u_outline: float
  - u_outlineColour: vec4
attributes:
  - a_position: vec4   # xy = position, zw = relative pivot [0..1]
  - a_size: vec4       # xy = size (px), z = rotation (radians), w = sprite rotated? (1 if 90 degrees rotated)
  - a_color: vec4      # rgba
  - a_texCoord0: vec4  # xy = top-left, zw = bottom-right
  - a_vertPos: vec2    # xy = relative position of vertex [0..1]
passes:
  - blend: Alpha
    vertex: sprite.vertex.glsl
    pixel: distance_field_sprite.pixel.glsl
...